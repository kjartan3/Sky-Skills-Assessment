// import { Behaviour, Skill } from "../../models";

// describe('Behaviour model', () => {
//   it('should associate with a skill', async () => {
//     const skill = await Skill.create({ name: 'Leadership' });
//     const behaviour = await Behaviour.create({ name: 'Takes initiative', skillId: skill.id });

//     const found = await behaviour.getSkill();
//     expect(found).toBeDefined();
//     expect(found.name).toBe('Leadership');
//   });

  
// });
